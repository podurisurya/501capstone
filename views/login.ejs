<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        *{
            margin: 0;
            padding: 0;
        }
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            font-family: Arial, sans-serif;
            overflow: hidden;
        }
        form {
            background: rgba(255, 255, 255, 0.8); /* Transparent background */
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 400px; /* Increased width */
            box-sizing: border-box;
            border: 2px solid rgb(6,6,64);
        }
        h1 {
            text-align: center;
            margin-bottom: 20px;
            font-size: 24px;
            color: #333;
        }
        label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
        }
        input[type="text"],
        input[type="number"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
        }
        input[type="text"]:focus,
        input[type="number"]:focus {
            border-color: #0066ff;
            outline: none;
        }
        p {
            margin: 0;
            text-align: center;
        }
        a {
            color: #0066ff;
            text-decoration: none;
        }
        a:hover {
            text-decoration: underline;
        }
        .button {
            background-color: rgb(6,6,64);
            color: white;
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            width: 100%;
            font-size: 16px;
        }
        .button:hover {
            background-color: ora;
        }
        .form-group {
            margin-bottom: 20px;
        }
        .diagonalimg{
            width: 100%;
            height: 100vh;
            background-image: linear-gradient(to top left, rgb(6,6,64) 50%, white 50%);
            padding-left: 500px;
            padding-top: 300px;
            
        }
    </style>
</head>
<body>
    <div class="diagonalimg">
    <form method="post" action="/login"> 
        <div class="form-group">
            <h1>LOGIN FORM</h1>
            <label>Email Address</label>
            <input type="text" name="email">
            <label>Password</label>
            <input type="number" name="number">
            <label class="label" for="userType"><b>APPLICANT:</b></label>
                <select id="option" name="userType">
                    <option value="" selected disabled>Applicant</option>
                    <option value="1">admin</option>
                    <option value="2">player</option>
                </select><br><br>
        </div>
        <div class="form-group">
            <button type="submit" class="button">Login</button>
        </div>
        <div>
            <p>Don't have an account? <a href="/register">Create Account</a></p>
        </div>
    </form>
</div>
</body>
</html>
